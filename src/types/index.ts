export type Breed = string;
export type SubBreed = string;
export type DogImageUrl = string;

export enum STATUS {
  IDLE = 'idle',
  FETCHING = 'fetching',
  SUCCESS = 'success',
  FAILURE = 'failure',
}
